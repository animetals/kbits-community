# Kalimba MIDI library

This folder intentionally contains no songs. Add your exclusive `.mid` or `.midi` files here later. For local use, songs can always be loaded with the upload button or by dragging them onto the player.
